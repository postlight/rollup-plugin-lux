image: Visual Studio 2015
platform: x64
environment:
  nodejs_version: 6
  NODE_ENV: test
services:
  - mysql
install:
  - ps: Install-Product node $env:nodejs_version $env:platform
  - npm install
test_script:
  - npm run flow
  - npm run lint
  - npm test
build: off
